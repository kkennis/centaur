diff --git a/dist/index.js b/dist/index.js
--- a/dist/index.js
+++ b/dist/index.js
@@ -1553,6 +1553,7 @@
   constructor(config) {
     this.userName = config.userName;
     this._stateAdapter = config.state;
+    this._onLockConflict = config.onLockConflict ?? "drop";
     this.adapters = /* @__PURE__ */ new Map();
     this._streamingUpdateIntervalMs = config.streamingUpdateIntervalMs ?? 500;
     this._fallbackStreamingPlaceholderText = config.fallbackStreamingPlaceholderText !== void 0 ? config.fallbackStreamingPlaceholderText : "...";
@@ -2489,15 +2490,25 @@
       });
       return;
     }
-    const lock = await this._stateAdapter.acquireLock(
+    let lock = await this._stateAdapter.acquireLock(
       threadId,
       DEFAULT_LOCK_TTL_MS
     );
     if (!lock) {
-      this.logger.warn("Could not acquire lock on thread", { threadId });
-      throw new LockError(
-        `Could not acquire lock on thread ${threadId}. Another instance may be processing.`
-      );
+      const resolution = typeof this._onLockConflict === "function"
+        ? this._onLockConflict(threadId, message)
+        : this._onLockConflict;
+      if (resolution === "force" && typeof this._stateAdapter.forceReleaseLock === "function") {
+        this.logger.info("Force-releasing lock on thread for interrupt", { threadId });
+        await this._stateAdapter.forceReleaseLock(threadId);
+        lock = await this._stateAdapter.acquireLock(threadId, DEFAULT_LOCK_TTL_MS);
+      }
+      if (!lock) {
+        this.logger.warn("Could not acquire lock on thread", { threadId });
+        throw new LockError(
+          `Could not acquire lock on thread ${threadId}. Another instance may be processing.`
+        );
+      }
     }
     this.logger.debug("Lock acquired", { threadId, token: lock.token });
     try {
